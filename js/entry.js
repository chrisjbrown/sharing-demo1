import { render } from 'vendor/react-dom';
import React from 'vendor/react';
import App from 'base/components/app';

const containerEl = document.getElementById('container');

render(
  <App/>,
  containerEl
);
